% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projectionStore.R
\name{pfm-projections}
\alias{pfm-projections}
\alias{saveProjection}
\alias{loadProjection}
\alias{listProjections}
\title{Persist, load, and list scenario projections (ADR 0009)}
\usage{
saveProjection(projection, label, dir = getOption("pfm.modelDir"), meta = NULL)

loadProjection(label, dir = getOption("pfm.modelDir"))

listProjections(dir = getOption("pfm.modelDir"))
}
\arguments{
\item{projection}{The projection object to persist (e.g. the data.frame from
\code{\link{predictFeasibility}}, or a list of them).}

\item{label}{Character. The label used at save time.}

\item{dir}{Character. Cache root. Defaults to \code{getOption("pfm.modelDir")}.}

\item{meta}{Optional named list of provenance (e.g. \code{gdx_path},
\code{scenario_data_hash}, \code{model_ids}); stored alongside the projection.}
}
\value{
The path written, invisibly.

For \code{loadProjection}: the stored projection (the \code{projection}
element), or \code{NULL} if absent.

For \code{listProjections}: a character vector of persisted labels.
}
\description{
Scenario projections are a \emph{disposable, gdx-dependent} artifact: a fitted
model plus a particular REMIND gdx yields one projection. They are kept out of
the (gdx-independent) Fitted Model and stored separately under
\code{{dir}/projections/} so that re-running a scenario, or coupling the same
model to a stream of updated gdx files, never rewrites the model. Persisting is
optional — in a REMIND iterative coupling the projection is typically fed
straight back without ever touching disk.
\itemize{
\item \code{saveProjection}: writes a projection under a caller-supplied label.
\item \code{loadProjection}: reads it back.
\item \code{listProjections}: lists persisted projection labels.
}
}
