% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computePSMCrossDataset.R
\name{computePSMCrossDataset}
\alias{computePSMCrossDataset}
\title{Cross-dataset concordance of CAPMF stringency with independent policy indices (T4)}
\usage{
computePSMCrossDataset(eps = NULL, climapp = NULL, capmf = NULL)
}
\arguments{
\item{eps, climapp}{optional data frames with columns \code{iso3}, \code{year}, and the index
value (\code{eps} / \code{climapp}). Pass \code{NULL} to skip a comparison.}

\item{capmf}{optional data frame \verb{iso3,year,capmf}; if \code{NULL}, sourced from
\code{mrpfm::calcOutput("PolicyStringency", aggregate = FALSE)} (composite, else bulk).}
}
\value{
A matrix (one row per available comparison) with columns
\code{within_country_r}, \code{cross_section_r}, \code{rank_concordance}.
}
\description{
Correlates the CAPMF-based policy-stringency composite with independent measures —
the OECD Environmental Policy Stringency (EPS) index and the CLIMAPP/ACCUPOL
portfolio dataset — at the overlapping country-year level (Steinebach et al. 2024),
to show the outcome is not an OECD-measurement idiosyncrasy. Compute lives in
\pkg{pfm} so the robustness analysis is executable without the paper repo; the
external datasets are passed in (read them via \pkg{mrpfm} readers or from file).
}
\author{
Renato Rodrigues
}
