% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psmCouplingWeights.R
\name{psmAssertSizeWeights}
\alias{psmAssertSizeWeights}
\title{Assert that aggregation weights behave like a size variable}
\usage{
psmAssertSizeWeights(w, what = "aggregation weights", minDispersion = 20)
}
\arguments{
\item{w}{Numeric weight vector.}

\item{what}{Label for the error message.}

\item{minDispersion}{Minimum acceptable max/median over the positive weights. Final energy
gives ~417 across 208 countries; equal weights give 1; the broken proxy gave 1.4. Anything
below ~20 is not a size variable.}
}
\value{
\code{w}, invisibly, or an error.
}
\description{
The check that would have caught the 2026-08-18 weight defect on day one, and did not exist.
\code{runPSMCouplingBound()} had been building weights as GDP x "Energy Intensity" read out of the
TRAINING PANEL — but those columns are normalised driver indices in \link{0, 1}
(\code{panelDataHistorical.R}: \code{energyIntensityNorm}), not levels. Their product is near-constant
across countries: max/median was \strong{1.4}, where real final energy is \strong{417}. Every
multi-country region was therefore aggregated with effectively equal country weights, and
China received a quarter of the CHA region instead of 96\%.

The same assertion catches the other silent failure in this family — a fallback to equal
weights, which yields a dispersion of exactly 1.
}
\author{
Renato Rodrigues
}
