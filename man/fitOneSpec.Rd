% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runFitGrid.R
\name{fitOneSpec}
\alias{fitOneSpec}
\title{Fit a single sweep specification (one sector × stage)}
\usage{
fitOneSpec(
  cfg,
  sector,
  stage,
  panelData,
  family = "gaussian",
  modelDir = NULL,
  forceRefit = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{cfg}{A normalised spec (named list) from the sweep config.}

\item{sector}{Character. \code{"Bulk"} or \code{"Diffuse"}.}

\item{stage}{Character. \code{"Adoption"} or \code{"Stringency"}.}

\item{panelData}{Prepared historical panel (magpie).}

\item{family}{Character. Stringency GLM family. Default \code{"gaussian"}.}

\item{modelDir}{Character or NULL. Fit Cache root.}

\item{forceRefit}{Logical. Ignore any cached fit and re-estimate. Default \code{FALSE}.}

\item{verbose}{Logical. Default \code{FALSE}.}
}
\value{
A list with \code{metrics} (one-row data.frame), \code{coefRows} (data.frame or
NULL), \code{failed} (logical), and \code{errMsg} (character or NULL).
}
\description{
The unit of work for the model sweep (ADR 0019). Fits one configuration for one
sector and stage, returns the metrics row and coefficient rows the selection needs, and
(when \code{modelDir} is set) persists the fit to the Fit Cache as \code{{id}.rds}
\emph{without} touching \code{index.json} — so it is safe to call concurrently from parallel
workers. The master rebuilds the index once after the sweep
(\code{\link{rebuildPFMModelIndex}}). The same function backs both the sequential and the
parallel execution paths.
}
\seealso{
\code{\link{runFitGrid}}, ADR 0019
}
\author{
Renato Rodrigues
}
