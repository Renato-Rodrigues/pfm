% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolveRegionMapping.R
\name{resolveRegionMapping}
\alias{resolveRegionMapping}
\title{resolveRegionMapping}
\usage{
resolveRegionMapping(outputRegionMappingFile)
}
\arguments{
\item{outputRegionMappingFile}{character; a region mapping file name or the
sentinel \code{"country"}.}
}
\value{
The concrete mapping file name (character).
}
\description{
Resolves the \code{outputRegionMappingFile} sentinel value \code{"country"}
to the in-code country-identity mapping (\code{RegionCode == CountryCode}), generated
on demand by \code{\link[mrpfm:toolCountryIdentityMapping]{mrpfm::toolCountryIdentityMapping()}}. Any other value is
returned unchanged, so every existing call site keeps working. With the
identity mapping, \code{aggregate = TRUE} aggregates each country to itself and the
whole pipeline runs at country resolution - no hand-made mapping file needed.

The panel builders combine this with an internal global-config scope
(see \code{.scopeRegionmapping}): \code{mrpfm::calcPolicyStringency()}'s coverage filter
reads the GLOBAL \code{madrat::getConfig("regionmapping")}, not the mapping passed
to \code{calcOutput()}, so without the scope a country-level run silently deletes
covered countries (Turkey, Greece, Chile, Korea, ...) through whole-region
coverage exclusion.
}
\seealso{
\code{\link[mrpfm:toolCountryIdentityMapping]{mrpfm::toolCountryIdentityMapping()}}
}
\author{
Renato Rodrigues
}
