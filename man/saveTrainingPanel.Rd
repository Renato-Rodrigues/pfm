% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/savePFMModel.R
\name{saveTrainingPanel}
\alias{saveTrainingPanel}
\title{Save the shared Training Panel once (content-addressed)}
\usage{
saveTrainingPanel(panel, dir = getOption("pfm.modelDir"))
}
\arguments{
\item{panel}{A magpie object (the raw historical panel) or data.frame.}

\item{dir}{Character. Cache root. Defaults to \code{getOption("pfm.modelDir")}.}
}
\description{
Writes the raw historical panel to \code{{dir}/panels/panel_{hash}.rds} only
if a file with that hash does not already exist, and sets
\code{options(pfm.trainingPanelHash = hash)} so that Fitted Models saved during
the same session reference it (ADR 0009). Returns the hash invisibly.
}
