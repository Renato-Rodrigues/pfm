% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterativePFM.R
\name{.psmCouplingOptimalPath}
\alias{.psmCouplingOptimalPath}
\title{The cost-optimal price path for the mode-2 bound, taken from a symbol the cap
cannot touch.}
\usage{
.psmCouplingOptimalPath(gdx, pR, TCO2, say = function(...) NULL)
}
\arguments{
\item{gdx}{The run's own gdx.}

\item{pR}{The reference price path (magpie), used for the region list.}

\item{TCO2}{Unit conversion from T$/GtC to US$/tCO2.}

\item{say}{Progress reporter.}
}
\value{
A long data.frame of \code{region, year, value} in US$/tCO2.
}
\description{
\code{p45_taxCO2eq_anchor(ttot)} is the uncapped global anchor every regional price
is built from, so it is the cost-optimal counterfactual the political share is meant
to discount. It carries no region dimension, so it is broadcast across the regions of
the reference path. Falling back to the run's own \code{pm_taxCO2eq} restores the
ratchet documented at the call site, so the fallback WARNS rather than passing
quietly - a run that takes it is not quotable.
}
\author{
Renato Rodrigues
}
\keyword{internal}
