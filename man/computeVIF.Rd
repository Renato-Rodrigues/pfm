% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeVIF.R
\name{computeVIF}
\alias{computeVIF}
\title{computeVIF}
\usage{
computeVIF(data, formula, excludePattern = "^regionFE|_x_", vifThreshold = 10)
}
\arguments{
\item{data}{A data.frame containing the predictor columns.}

\item{formula}{A \code{stats::formula} whose RHS defines the predictors to evaluate.}

\item{excludePattern}{Character. Regex pattern for columns to exclude from
multicollinearity checks (e.g., regionFE dummies). Defaults to \code{"^regionFE|_x_"}.}

\item{vifThreshold}{Numeric. Threshold above which VIF is considered
indicating high multicollinearity. Defaults to \code{10}.}
}
\value{
A named list containing:
\describe{
\item{values}{Named numeric vector of VIF values per predictor.}
\item{maxVIF}{The absolute maximum VIF value observed.}
\item{highVIF}{Logical: \code{TRUE} if any calculated VIF exceeds the
\code{vifThreshold}, \code{FALSE} otherwise.}
\item{flagged}{Character vector containing the names of predictors exceeding the threshold.}
}
Returns \code{NULL} if VIF cannot be reliably computed.
}
\description{
Computes Variance Inflation Factors (VIF) for the predictors of
a linear model, without requiring external dependencies like \code{car}.
}
\author{
Renato Rodrigues
}
\keyword{internal}
