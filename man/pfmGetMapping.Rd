% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pfmGetMapping.R
\name{pfmGetMapping}
\alias{pfmGetMapping}
\title{Resolve a region mapping, preferring the configured mappingfolder}
\usage{
pfmGetMapping(name, type = "regional", verbose = TRUE)
}
\arguments{
\item{name}{Mapping file name, e.g. \code{"regionmapping_21_EU11.csv"}.}

\item{type}{madrat mapping type; \code{"regional"} for region mappings.}

\item{verbose}{Logical. Report when the bundled fallback is used.}
}
\value{
The mapping data.frame.
}
\description{
Delegates to \code{\link[mrpfm]{toolPFMMapping}}. The mappings and the resolution
logic live in \pkg{mrpfm} — the data layer, which \pkg{pfm} depends on — so there
is exactly one copy of each mapping and one search order for the whole stack.

This wrapper exists so pfm's own call sites read naturally and do not each have to
name the data package. See \code{\link[mrpfm]{toolPFMMapping}} for the search order
and for why it is closed rather than deferring to madrat's package search.
}
\seealso{
\code{\link[mrpfm]{toolPFMMapping}}
}
\author{
Renato Rodrigues
}
