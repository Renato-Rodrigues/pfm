% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runPostProcessing.R
\name{runTemporalSplit}
\alias{runTemporalSplit}
\title{Temporal-split (out-of-time) post-processing for a Run-Group (ADR 0015)}
\usage{
runTemporalSplit(
  group,
  resultsDir = getOption("pfm.resultsDir", NULL),
  modelDir = getOption("pfm.modelDir", NULL),
  cachefolder = NULL,
  maxYear = 2023,
  outputRegionMappingFile = "regionmapping_54.csv",
  panelData = NULL,
  verbose = TRUE
)
}
\arguments{
\item{group, resultsDir, modelDir}{Run-Group locators.}

\item{maxYear}{Integer. Latest year to extend the panel to (falls back to 2022). Default 2023.}

\item{outputRegionMappingFile}{Panel region mapping.}

\item{panelData}{Optional pre-built (extended) panel; built when NULL.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The temporal-split artifact (invisibly).
}
\description{
Runs \code{\link{computeTemporalSplit}} on the selected deliverable specs over the panel
extended to the latest driver-supported year. Writes \code{<group>/temporal-split.rds}.
}
\seealso{
ADR 0015, \code{\link{computeTemporalSplit}}
}
