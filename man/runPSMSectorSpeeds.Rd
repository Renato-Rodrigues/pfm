% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runPSMSectorSpeeds.R
\name{runPSMSectorSpeeds}
\alias{runPSMSectorSpeeds}
\title{Four-sector ratcheting speeds (electricity / industry / buildings / transport)}
\usage{
runPSMSectorSpeeds(
  group,
  resultsDir = getOption("pfm.resultsDir", "output"),
  modelDir = getOption("pfm.modelDir", "output"),
  cachefolder = NULL,
  panelData = NULL,
  y = 2000:2022,
  trainEnd = 2015,
  sectors = c("Electricity", "Industry", "Buildings", "Transport"),
  outputRegionMappingFile = "regionmapping_54.csv",
  indexMax = 10,
  verbose = TRUE
)
}
\arguments{
\item{group}{Character. PSM Run-Group name.}

\item{resultsDir, modelDir, cachefolder}{As in \code{\link{runPSMProjection}}.}

\item{panelData}{Optional pre-built historical panel (with the PSM outcomes).}

\item{y}{Numeric vector. Full panel years. Default \code{2000:2022}.}

\item{trainEnd}{Numeric. Last training year; later years are held out.
Default \code{2015} (7 held-out years).}

\item{sectors}{Character vector of four-sector outcome names. Default
\code{c("Electricity","Industry","Buildings","Transport")}.}

\item{outputRegionMappingFile, indexMax, verbose}{As in
\code{\link{runPSMProjection}}.}
}
\value{
Invisibly, a list \code{list(spec, trainEnd, bySector = list(<sector>
  = list(metrics, rows)))}; also writes \code{<group>/sector-speeds.rds}.
}
\description{
The sector-disaggregated extension of the validated Bulk/Diffuse speed result
(2026-07-08). CAPMF natively separates electricity, industry, buildings and
transport, and the OECD's own sectoral analysis shows they advance at
different rates; this step estimates a validated error-correction adjustment
speed for each of the four, applying the Run-Group's deployed shared
specification to each sector outcome. For every sector it refits on the
training window, forecasts the held-out years recursively (the same protocol
as \code{\link{runPSMTemporalValidation}}'s ECM form) and reports the speed,
half-life and skill against the persistence benchmark.
}
\seealso{
\code{\link{runPSMTemporalValidation}}, ADR 0036
}
\author{
Renato Rodrigues
}
