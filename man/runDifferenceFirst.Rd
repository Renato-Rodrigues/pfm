% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runPostProcessing.R
\name{runDifferenceFirst}
\alias{runDifferenceFirst}
\title{Difference-First comparison post-processing for a Run-Group (ADR 0014)}
\usage{
runDifferenceFirst(
  group,
  resultsDir = getOption("pfm.resultsDir", NULL),
  cacheDir = getOption("pfm.modelDir", NULL),
  gdxFile = NULL,
  panelData = NULL,
  scenarioData = NULL,
  maxTries = 25L,
  y = 2000:2022,
  outputRegionMappingFile = "regionmapping_54.csv",
  verbose = TRUE
)
}
\arguments{
\item{group, resultsDir, cacheDir}{Run-Group locators.}

\item{gdxFile}{Character or NULL. Scenario gdx (enables the levels FE-by-sanity choice).}

\item{panelData, scenarioData}{Optional pre-built panels.}

\item{maxTries}{Integer. Max ranked hybridFD candidates to falsification-test. Default 25.}

\item{y, outputRegionMappingFile}{Panel build parameters.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The difference-first diagnostics list (invisibly).
}
\description{
Post-hoc consumer of the Run-Group sweep: filters to hybridFD, ranks with maximin, applies
the Falsification Gate (pureFD re-fit), and configures the winner for LEVELS estimation. Also
runs the gate on the levels-first deliverable for comparison. Writes
\code{<group>/difference-first.rds} (diagnostics for the report) and the difference-first
selected-models YAML. Does not re-fit the sweep.
}
\seealso{
ADR 0014, \code{\link{selectDifferenceFirst}}, \code{\link{computeFalsificationGate}}
}
