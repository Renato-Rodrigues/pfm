% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runSweep.R
\name{runSweep}
\alias{runSweep}
\title{Run a model sweep into a Run-Group}
\usage{
runSweep(
  group,
  mode = c("exhaustive", "guided"),
  resultsDir = getOption("pfm.resultsDir", NULL),
  modelDir = getOption("pfm.modelDir", NULL),
  cachefolder = NULL,
  gdxFile = NULL,
  panelData = NULL,
  scenarioData = NULL,
  y = 2000:2022,
  outputRegionMappingFile = "regionmapping_54.csv",
  movingAverage = 5,
  sectors = c("Bulk", "Diffuse"),
  nCores = 1L,
  forceRefit = FALSE,
  selectionMethod = c("levels-first", "difference-first"),
  family = "gaussian",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{group}{Character. Run-Group name (e.g. \code{"exhaustive"}, \code{"guided"}, or a
custom experiment name). Required.}

\item{mode}{Character. \code{"exhaustive"} or \code{"guided"}. Default \code{"exhaustive"}.}

\item{resultsDir}{Character. Results Root (configurable). Default
\code{getOption("pfm.resultsDir")}. The Run-Group is \code{file.path(resultsDir, group)}.}

\item{modelDir}{Character. Fit Cache root (configurable; ADR 0009 model store — distinct
from the madrat \code{cachefolder}). Default \code{getOption("pfm.modelDir")}; also set as
\code{options(pfm.modelDir=)} for the run.}

\item{cachefolder}{Character or NULL. The \strong{madrat} data-cache folder. When non-NULL,
\code{madrat::setConfig(cachefolder = ...)} so the panel/CarbonPrice load from there. NULL
(default) leaves the ambient madrat config untouched (only \code{forcecache} is set).}

\item{gdxFile}{Character or NULL. REMIND \code{fulldata.gdx} for the scenario panel
(enables the Projection Sanity gate). When NULL/missing, the gate is skipped.}

\item{panelData, scenarioData}{Optional pre-built panels (skip the build step).}

\item{y}{Integer vector of training years. Default \code{2000:2022}.}

\item{outputRegionMappingFile}{Character. Region mapping. Default \code{"regionmapping_54.csv"}.}

\item{movingAverage}{Integer. Passed to \code{panelDataHistorical}. Default \code{5}.}

\item{sectors}{Character vector. Default \code{c("Bulk","Diffuse")}.}

\item{nCores}{Integer. Cores for the fit sweep (ADR 0019). Default \code{1}.}

\item{forceRefit}{Logical. Ignore cached fits and re-estimate. Default \code{FALSE}.}

\item{selectionMethod}{Character. \code{"levels-first"} (default) or \code{"difference-first"}.}

\item{family}{Character. Stringency GLM family. Default \code{"gaussian"}.}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{...}{Further arguments forwarded to \code{\link{runChannelsWorkflow}} (e.g.
\code{sanityThresholds}, \code{selectFE}, \code{nearTieEps}).}
}
\value{
The workflow result list (invisibly); see \code{\link{runChannelsWorkflow}}.
}
\description{
Compute-layer entry point (ADR 0018). Builds/loads the historical panel (and, if a gdx is
given, the scenario panel), runs the full specification sweep + maximin / Projection-Sanity
selection (parallel-capable, ADR 0019), and writes the curated artifacts into
\code{<resultsDir>/<group>/}: \code{sweep.rds}, \code{selected-models.yml} (or
\code{selected-models-difference-first.yml}), the sweep config, and \code{manifest.json}.
Renders no reports — that is the report layer's job (pfm-reports reads this Run-Group).
}
\seealso{
\code{\link{runChannelsWorkflow}}, \code{\link{runModelGroup}}, ADR 0018, ADR 0019
}
\author{
Renato Rodrigues
}
