% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runPSMPostProcessing.R
\name{runPSMEstimatorAgreement}
\alias{runPSMEstimatorAgreement}
\title{Estimator-agreement Run-Group step for the deployed PSM spec}
\usage{
runPSMEstimatorAgreement(
  group,
  resultsDir = getOption("pfm.resultsDir", "output"),
  modelDir = getOption("pfm.modelDir", "output"),
  cachefolder = NULL,
  panelData = NULL,
  y = 2000:2022,
  outputRegionMappingFile = "regionmapping_54.csv",
  indexMax = 10,
  estimators = c("satP", "fractional", "beta", "levels"),
  verbose = TRUE
)
}
\arguments{
\item{group}{Character. PSM Run-Group name (from \code{\link{runPSMSweep}}).}

\item{resultsDir, modelDir, cachefolder}{As in \code{\link{runProjection}}.}

\item{panelData}{Optional pre-built historical panel (must carry the Policy
Stringency outcomes).}

\item{y, outputRegionMappingFile}{As in \code{\link{runProjection}}.}

\item{indexMax}{Numeric. Index ceiling. Default \code{10}.}

\item{estimators}{Character vector of suite members to fit.}

\item{verbose}{Logical.}
}
\value{
Invisibly, a list with one \code{\link{computeEstimatorAgreement}}
result per sector plus the deployed spec name, or \code{NULL} when skipped.
}
\description{
Post-selection exhibit (ADR 0036): re-estimates the Run-Group's deployed Policy
Stringency spec under every member of the PSM Estimator Suite per sector
(\code{\link{computeEstimatorAgreement}} — satP engine, fractional-logit
headline, beta-regression cross-check, levels benchmark) and writes
\code{<group>/estimator-agreement.rds}. This is the paper's
estimator-invariance claim: signs/AMEs of the selected spec across the suite,
compared only on scale-free natural-scale metrics, never on AIC/BIC.
}
\seealso{
\code{\link{computeEstimatorAgreement}}, ADR 0036
}
\author{
Renato Rodrigues
}
