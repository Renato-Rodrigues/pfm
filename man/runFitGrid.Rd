% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runFitGrid.R
\name{runFitGrid}
\alias{runFitGrid}
\title{Fit a full sweep grid (specs × sectors × stages), optionally in parallel}
\usage{
runFitGrid(
  specs,
  sectors,
  stages,
  panelData,
  family = "gaussian",
  modelDir = NULL,
  nCores = 1L,
  forceRefit = FALSE,
  verbose = TRUE,
  say = NULL
)
}
\arguments{
\item{specs}{List of normalised specs.}

\item{sectors}{Character vector (e.g. \code{c("Bulk","Diffuse")}).}

\item{stages}{Character vector (e.g. \code{c("Adoption","Stringency")}).}

\item{panelData}{Prepared historical panel (magpie).}

\item{family}{Character. Stringency GLM family. Default \code{"gaussian"}.}

\item{modelDir}{Character or NULL. Fit Cache root.}

\item{nCores}{Integer. \code{1} (default) runs sequentially; \code{> 1} uses
\pkg{future.apply} if installed (else falls back to sequential with a warning).}

\item{forceRefit}{Logical. Ignore cached fits and re-estimate every spec. Default
\code{FALSE} (resume: cached fits are loaded, only missing ones computed).}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{say}{Optional logging function \code{function(...)}; defaults to a \code{[fits]} prefix.}
}
\value{
A list: \code{results} (per-fit metrics data.frame), \code{coefficients}
(rbind of coef rows or NULL), \code{nJobs}, \code{nNew} (newly written fits),
\code{nFailed}.
}
\description{
Flattens the specification grid into independent jobs and maps \code{\link{fitOneSpec}}
over them — sequentially (\code{nCores = 1}) or in parallel via \pkg{future.apply}
(\code{plan(multisession)} on Windows, \code{plan(multicore)} on Unix). Workers persist
their own \code{{id}.rds} without touching the index; the master rebuilds \code{index.json}
once afterwards (ADR 0019). Returns the assembled metrics and coefficient tables.
}
\seealso{
\code{\link{fitOneSpec}}, \code{\link{rebuildPFMModelIndex}}, ADR 0019
}
\author{
Renato Rodrigues
}
