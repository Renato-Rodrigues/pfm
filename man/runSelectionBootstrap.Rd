% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runSelectionBootstrap.R
\name{runSelectionBootstrap}
\alias{runSelectionBootstrap}
\title{Selection-uncertainty bootstrap (ADR 0025)}
\usage{
runSelectionBootstrap(
  group,
  resultsDir = getOption("pfm.resultsDir", "output"),
  modelDir = getOption("pfm.modelDir", "output"),
  cachefolder = NULL,
  y = 2000:2022,
  outputRegionMappingFile = "regionmapping_54.csv",
  nResamples = 200L,
  detail = c("channel", "full"),
  topK = 40L,
  family = "gaussian",
  seed = 1L,
  verbose = TRUE
)
}
\arguments{
\item{group}{Character. Run-Group name.}

\item{resultsDir, modelDir}{Results Root / Fit Cache. Defaults from options.}

\item{cachefolder}{Character or NULL. madrat data cache (set in-session when supplied).}

\item{y, outputRegionMappingFile}{Panel build args (\code{\link{panelDataHistorical}}).}

\item{nResamples}{Integer. Number of bootstrap resamples. Default \code{200}.}

\item{detail}{\code{"channel"} (default — channel-set + per-channel frequency) or \code{"full"}
(also exact-spec selection frequency, for the publication run).}

\item{topK}{Integer. Size of the candidate pool per stage (top gate-passing maximin specs).
Default \code{40}.}

\item{family}{GLM family for stringency. Default \code{"gaussian"}.}

\item{seed}{Integer. RNG seed. Default \code{1}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
Invisibly, the bootstrap summary list (also saved as \code{selection-bootstrap.rds}).
}
\description{
Region-block (cluster) bootstrap of the Maximin selection. For each resample the 54 regions are
drawn with replacement, the gate-passing candidate pool is refit on the resampled design (via
the \code{prepared = TRUE} entry — duplicated region rows just add weight to their block, no FE
relabel needed), re-ranked by \code{\link{computeMaximinScore}}, and the winner's \emph{channel
set} (and, with \code{detail = "full"}, the exact spec) is recorded. Quantifies how stable the
deliverable's channel choice is to the specification search. Writes
\code{selection-bootstrap.rds} to the Run-Group. Heavy and cluster-oriented; the default
\code{detail = "channel"} reports channel-set stability (the scientific claim), which is far
more stable than the exact winning row.
}
\seealso{
\code{\link{computeMaximinScore}}, ADR 0025
}
\author{
Renato Rodrigues
}
